<h3>Selected parts of cookie</h3>
<ul>
    <li> Base: {{cookie.base}} - {{pricify (findPrice allBases cookie.base)}} PLN </li>

    {{#each cookie.addons}}
<!--        @root.addons po to aby wyjsc z kontektu each do glownego kontekstu-->
        <li>
            Addon: {{this}} - {{pricify (findPrice @root.allAddons this)}} PLN
            {{#unless ../noDeleteBtn}} <a href="/configurator/delete-addon/{{this}}" class="btn btn-small">Delete</a>  {{/unless}}
        </li>
    {{/each}}
</ul>

<p>
    <strong>Total of {{pricify sum}} PLN</strong>
</p>
